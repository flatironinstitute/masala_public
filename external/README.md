External libraries go here (preferably as git submodules).

Current submodules:
- Catch2 (for unit testing).
- Eigen (a linear algebra package).
- Nlohmann\_JSON (a library for parsing and writing JSON).:
